[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rdf_parser_rust"
requires-python = ">=3.9"
version = "0.1.0"

[tool.uv]
exclude-newer = "P14D"

[tool.maturin]
features = ["pyo3/extension-module"]
